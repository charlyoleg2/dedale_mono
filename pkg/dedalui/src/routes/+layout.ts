// +layout.ts [root]

export const prerender = false; // default: false. if true, generate pages at build time and disable ssr-mode
export const ssr = true; // default: true. if true, ssr-mode. if false, csr-mode
export const csr = true; // default: true. if false, remove javascript from page
