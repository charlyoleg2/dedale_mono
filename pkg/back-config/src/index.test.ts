// index.test.ts of back-config
import { describe, it, expect } from 'vitest';
//import { backCfg } from './index.js';

describe('tests of back-config', () => {
	it('dummy-test', () => {
		expect((50 + 25)).toBe(75);
	});
});
